<div class="jumbotron">

  <%= image_tag("thavorn_beach.png", class:  "hero-image") %>
  <div class="center-block">
    <%= link_to  listings_path do %>
    <h1 class="welcome">Destinations</h1>

      <% if current_user %>
        <button type="button" class="btn btn-default create-listing">
          <div class="active"><%= link_to "Create A Listing", new_user_listing_path(current_user.slug), id: "new_listing" %></div>
        </button>
      <% end %>
    <% end %>
  </div>
</div>

<%= render "home_page_search_bar" %>
<%= render "home_page_listings" %>
